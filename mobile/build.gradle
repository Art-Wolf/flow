apply plugin: 'com.android.application'

android {
    compileSdkVersion 9
    buildToolsVersion '20.0.0'

    defaultConfig {
        applicationId "ie.johndoyle.flow"
        minSdkVersion 19
        targetSdkVersion 20
        versionCode 1
        versionName "1.0"
    }
    signingConfigs {
        release {
            storeFile file("/Users/johndoyle/keystore2.jks")
            storePassword "H4rdR0ckC4fe"
            keyAlias "something"
            keyPassword "H4rdR0ckC4fe"
        }
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
            signingConfig signingConfigs.release
        }
    }
}

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
    wearApp files('/Users/johndoyle/AndroidStudioProjects/Flow/wear/build/outputs/apk/wear-release.apk')
}
